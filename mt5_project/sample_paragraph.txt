PyTorch is an open-source machine learning framework used for building and training neural networks. It is popular because it represents data as tensors, supports automatic differentiation for computing gradients, and lets developers run the same code on a CPU or a GPU with minimal changes. Many modern AI models, including large language models, are built and trained using PyTorch.
